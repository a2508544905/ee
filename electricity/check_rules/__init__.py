# check_rules/
# AST 静态语法规则检查包
#
# 作用：在不运行代码的前提下，用语法树扫描源码，强制拦截违反项目约定的写法。
# 设计：
#   - 每条规则是一个独立函数，接收 AST 节点，返回问题描述列表（空表示通过）
#   - 规则作用于哪个目录，用 scopes（路径关键字）表示，避免误伤测试/入口
#   - 由 check.py 遍历目录后统一汇总，任何问题都会使程序以非零码退出